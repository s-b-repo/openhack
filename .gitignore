.DS_Store
node_modules
.worktrees
.sst
.env
.env.local
.idea
.vscode
.codex
*~
playground
tmp
dist
ts-dist
.turbo
**/.serena
.serena/
**/.omo
.omo/
/result
refs
Session.vim
/openhack.json
a.out
target
.scripts
.direnv/

# Vendored third-party reference repositories (cloned via Phase 1 of the
# Cloud Codes integration; see vendor/README.md). Not part of the build.
vendor/

# Local dev files
openhack-dev
UPCOMING_CHANGELOG.md
logs/
*.bun-build
tsconfig.tsbuildinfo

# OpenHack per-engagement runtime state — keep only the config template, the
# agent/command/skill definitions, and dev tooling; never commit findings, logs,
# secrets, ROE, or scope data.
.openhack/*
!.openhack/openhack.jsonc
!.openhack/agents/
!.openhack/agent/
!.openhack/command/
!.openhack/skills/
!.openhack/glossary/
!.openhack/themes/
!.openhack/plugins/
!.openhack/tool/
!.openhack/tui.json
!.openhack/env.d.ts
