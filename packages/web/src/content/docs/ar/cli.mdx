---
title: CLI
description: أوامر وخيارات CLI في OpenHack.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

يبدأ CLI الخاص بـ OpenHack افتراضيا واجهة [TUI](/docs/tui) عند تشغيله بدون أي معاملات.

```bash
openhack
```

كما يدعم أيضا أوامر موثقة في هذه الصفحة، ما يتيح لك التفاعل مع OpenHack برمجيا.

```bash
openhack run "Explain how closures work in JavaScript"
```

---

### tui

بدء واجهة المستخدم terminal (TUI) الخاصة بـ OpenHack.

```bash
openhack [project]
```

#### الخيارات

| الخيار                                   | المختصر | الوصف                                                             |
| ---------------------------------------- | ------- | ----------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr> | `-c`    | متابعة الجلسة الأخيرة                                             |
| <nobr><code>{"--session"}</code></nobr>  | `-s`    | معرّف الجلسة للمتابعة                                             |
| <nobr><code>{"--fork"}</code></nobr>     |         | تفريع الجلسة عند المتابعة (يستخدم مع `--continue` أو `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |         | الموجّه المراد استخدامه                                           |
| <nobr><code>{"--model"}</code></nobr>    | `-m`    | النموذج المراد استخدامه بصيغة provider/model                      |
| <nobr><code>{"--agent"}</code></nobr>    |         | الوكيل المراد استخدامه                                            |
| <nobr><code>{"--port"}</code></nobr>     |         | المنفذ الذي يتم الاستماع عليه                                     |
| <nobr><code>{"--hostname"}</code></nobr> |         | اسم المضيف الذي يتم الاستماع عليه                                 |

---

## الأوامر

يتضمن CLI الخاص بـ OpenHack أيضا الأوامر التالية.

---

### agent

إدارة وكلاء OpenHack.

```bash
openhack agent [command]
```

---

### attach

إرفاق terminal بخادم الواجهة الخلفية لـ OpenHack قيد التشغيل بالفعل، والذي تم تشغيله عبر الأمرين `serve` أو `web`.

```bash
openhack attach [url]
```

يتيح ذلك استخدام واجهة TUI مع واجهة خلفية لـ OpenHack تعمل عن بعد. على سبيل المثال:

```bash
# ابدأ خادم الواجهة الخلفية للوصول عبر الويب/الجوال
openhack web --port 4096 --hostname 0.0.0.0

# في محطة طرفية (terminal) أخرى، اربط TUI بالواجهة الخلفية قيد التشغيل
openhack attach http://10.20.30.40:4096
```

#### الرايات

| الراية                                   | المختصر | الوصف                                                                             |
| ---------------------------------------- | ------- | --------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |         | دليل العمل الذي ستبدأ منه واجهة TUI                                               |
| <nobr><code>{"--continue"}</code></nobr> | `-c`    | متابعة آخر جلسة                                                                   |
| <nobr><code>{"--session"}</code></nobr>  | `-s`    | معرّف الجلسة للمتابعة                                                             |
| <nobr><code>{"--fork"}</code></nobr>     |         | تفريع الجلسة عند المتابعة (استخدمه مع `--continue` أو `--session`)                |
| <nobr><code>{"--password"}</code></nobr> | `-p`    | كلمة مرور المصادقة الأساسية (الافتراضي `OPENHACK_SERVER_PASSWORD`)                |
| <nobr><code>{"--username"}</code></nobr> | `-u`    | اسم مستخدم المصادقة الأساسية (الافتراضي `OPENHACK_SERVER_USERNAME` أو `openhack`) |

---

#### create

إنشاء وكيل جديد بإعدادات مخصصة.

```bash
openhack agent create
```

سيرشدك هذا الأمر خلال إنشاء وكيل جديد مع موجه نظام مخصص وإعدادات الأدوات.

---

#### list

عرض جميع الوكلاء المتاحين.

```bash
openhack agent list
```

---

### auth

أمر لإدارة بيانات الاعتماد وتسجيل الدخول لمزودي الخدمة.

```bash
openhack auth [command]
```

---

#### login

يعتمد OpenHack على قائمة المزودين في [Models.dev](https://models.dev)، لذا يمكنك استخدام `openhack auth login` لتهيئة مفاتيح API لأي مزود ترغب باستخدامه. يتم حفظ ذلك في `~/.local/share/openhack/auth.json`.

```bash
openhack auth login
```

عند تشغيل OpenHack يقوم بتحميل المزودين من ملف بيانات الاعتماد، وكذلك أي مفاتيح معرّفة في متغيرات البيئة لديك أو في ملف `.env` ضمن مشروعك.

---

#### list

يسرد جميع المزودين الذين تم توثيقهم كما هم محفوظون في ملف بيانات الاعتماد.

```bash
openhack auth list
```

أو النسخة المختصرة.

```bash
openhack auth ls
```

---

#### logout

يسجلك خارج مزود عبر حذفه من ملف بيانات الاعتماد.

```bash
openhack auth logout
```

---

### github

إدارة وكيل GitHub لأتمتة المستودع.

```bash
openhack github [command]
```

---

#### install

تثبيت وكيل GitHub في مستودعك.

```bash
openhack github install
```

يقوم ذلك بإعداد سير عمل GitHub Actions اللازم ويرشدك خلال عملية التهيئة. [اعرف المزيد](/docs/github).

---

#### run

تشغيل وكيل GitHub. يُستخدم هذا عادة ضمن GitHub Actions.

```bash
openhack github run
```

##### الرايات

| الراية                                | الوصف                                |
| ------------------------------------- | ------------------------------------ |
| <nobr><code>{"--event"}</code></nobr> | حدث GitHub مُحاكى لتشغيل الوكيل عليه |
| <nobr><code>{"--token"}</code></nobr> | رمز وصول شخصي لـ GitHub              |

---

### mcp

إدارة خوادم Model Context Protocol.

```bash
openhack mcp [command]
```

---

#### add

إضافة خادم MCP إلى تهيئتك.

```bash
openhack mcp add
```

سيرشدك هذا الأمر خلال إضافة خادم MCP محلي أو بعيد.

---

#### list

عرض جميع خوادم MCP المُهيأة وحالة اتصالها.

```bash
openhack mcp list
```

أو استخدم النسخة المختصرة.

```bash
openhack mcp ls
```

---

#### auth

إجراء المصادقة مع خادم MCP يدعم OAuth.

```bash
openhack mcp auth [name]
```

إذا لم تُحدِّد اسم خادم، فسيُطلب منك الاختيار من الخوادم المتاحة الداعمة لـ OAuth.

يمكنك أيضا عرض الخوادم الداعمة لـ OAuth وحالة المصادقة الخاصة بها.

```bash
openhack mcp auth list
```

أو استخدم النسخة المختصرة.

```bash
openhack mcp auth ls
```

---

#### logout

إزالة بيانات اعتماد OAuth لخادم MCP.

```bash
openhack mcp logout [name]
```

---

#### debug

تشخيص مشاكل اتصال OAuth لخادم MCP.

```bash
openhack mcp debug <name>
```

---

### models

عرض جميع النماذج المتاحة من المزودين المُهيأين.

```bash
openhack models [provider]
```

يعرض هذا الأمر جميع النماذج المتاحة عبر المزودين المُهيأين لديك بصيغة `provider/model`.

هذا مفيد لتحديد اسم النموذج الدقيق الذي ستستخدمه في [تهيئتك](/docs/config/).

يمكنك اختياريا تمرير معرّف مزود لتصفية النماذج حسب ذلك المزود.

```bash
openhack models anthropic
```

#### الرايات

| الراية                                  | الوصف                                                         |
| --------------------------------------- | ------------------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | تحديث ذاكرة التخزين المؤقت للنماذج من models.dev              |
| <nobr><code>{"--verbose"}</code></nobr> | استخدام مخرجات أكثر تفصيلا للنماذج (تشمل بيانات مثل التكاليف) |

استخدم الراية `--refresh` لتحديث قائمة النماذج المخزنة مؤقتا. يفيد ذلك عند إضافة نماذج جديدة إلى مزود وتريد رؤيتها في OpenHack.

```bash
openhack models --refresh
```

---

### run

تشغيل openhack في وضع غير تفاعلي عبر تمرير موجه مباشرة.

```bash
openhack run [message..]
```

هذا مفيد للبرمجة النصية والأتمتة، أو عندما تريد إجابة سريعة دون تشغيل واجهة TUI كاملة. على سبيل المثال.

```bash "openhack run"
openhack run Explain the use of context in Go
```

يمكنك أيضا الإرفاق بمثيل `openhack serve` قيد التشغيل لتجنّب زمن الإقلاع البارد لخوادم MCP في كل تشغيل:

```bash
# ابدأ خادمًا بلا واجهة في محطة طرفية واحدة
openhack serve

# في محطة طرفية أخرى، شغّل الأوامر التي ترتبط به
openhack run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### الرايات

| الراية                                   | المختصر | الوصف                                                                             |
| ---------------------------------------- | ------- | --------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |         | الأمر المراد تشغيله؛ استخدم الرسالة كوسائط                                        |
| <nobr><code>{"--continue"}</code></nobr> | `-c`    | متابعة الجلسة الأخيرة                                                             |
| <nobr><code>{"--session"}</code></nobr>  | `-s`    | معرّف الجلسة للمتابعة                                                             |
| <nobr><code>{"--fork"}</code></nobr>     |         | تفريع الجلسة عند المتابعة (يستخدم مع `--continue` أو `--session`)                 |
| <nobr><code>{"--share"}</code></nobr>    |         | مشاركة الجلسة                                                                     |
| <nobr><code>{"--model"}</code></nobr>    | `-m`    | النموذج المراد استخدامه بصيغة provider/model                                      |
| <nobr><code>{"--agent"}</code></nobr>    |         | الوكيل المراد استخدامه                                                            |
| <nobr><code>{"--file"}</code></nobr>     | `-f`    | ملف/ملفات لإرفاقها بالرسالة                                                       |
| <nobr><code>{"--format"}</code></nobr>   |         | التنسيق: default (منسق) أو json (أحداث JSON خام)                                  |
| <nobr><code>{"--title"}</code></nobr>    |         | عنوان للجلسة (يستخدم موجهًا مقتطعًا إن لم تُحدَّد قيمة)                           |
| <nobr><code>{"--attach"}</code></nobr>   |         | الإرفاق بخادم openhack قيد التشغيل (مثل http://localhost:4096)                    |
| <nobr><code>{"--password"}</code></nobr> | `-p`    | كلمة مرور المصادقة الأساسية (الافتراضي `OPENHACK_SERVER_PASSWORD`)                |
| <nobr><code>{"--username"}</code></nobr> | `-u`    | اسم مستخدم المصادقة الأساسية (الافتراضي `OPENHACK_SERVER_USERNAME` أو `openhack`) |
| <nobr><code>{"--dir"}</code></nobr>      |         | دليل التشغيل، أو المسار على الخادم البعيد عند الإرفاق                             |
| <nobr><code>{"--variant"}</code></nobr>  |         | متغير النموذج (جهد الاستدلال الخاص بالمزود)                                       |
| <nobr><code>{"--thinking"}</code></nobr> |         | عرض كتل التفكير                                                                   |
| <nobr><code>{"--port"}</code></nobr>     |         | منفذ الخادم المحلي (الافتراضي منفذ عشوائي)                                        |

---

### serve

بدء خادم OpenHack بدون واجهة للوصول عبر API. راجع [توثيق الخادم](/docs/server) لواجهة HTTP الكاملة.

```bash
openhack serve
```

يشغّل هذا خادم HTTP يوفّر وصولا عبر API لوظائف openhack دون واجهة TUI. اضبط `OPENHACK_SERVER_PASSWORD` لتفعيل مصادقة HTTP الأساسية (اسم المستخدم الافتراضي `openhack`).

#### الرايات

| الراية                                   | الوصف                                     |
| ---------------------------------------- | ----------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | المنفذ الذي يتم الاستماع عليه             |
| <nobr><code>{"--hostname"}</code></nobr> | اسم المضيف الذي يتم الاستماع عليه         |
| <nobr><code>{"--mdns"}</code></nobr>     | تفعيل اكتشاف mDNS                         |
| <nobr><code>{"--cors"}</code></nobr>     | أصول/منشأات إضافية للمتصفح للسماح بـ CORS |

---

### session

إدارة جلسات OpenHack.

```bash
openhack session [command]
```

---

#### list

عرض جميع جلسات OpenHack.

```bash
openhack session list
```

##### الرايات

| الراية                                    | المختصر | الوصف                                 |
| ----------------------------------------- | ------- | ------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`    | حصر النتائج في أحدث N جلسات           |
| <nobr><code>{"--format"}</code></nobr>    |         | تنسيق المخرجات: table أو json (table) |

---

### stats

عرض إحصاءات استخدام الرموز والتكلفة لجلسات OpenHack لديك.

```bash
openhack stats
```

#### الرايات

| الراية                                  | الوصف                                                                     |
| --------------------------------------- | ------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | عرض الإحصاءات لآخر N يومًا (الافتراضي: كل الوقت)                          |
| <nobr><code>{"--tools"}</code></nobr>   | عدد الأدوات المطلوب عرضها (الافتراضي: الكل)                               |
| <nobr><code>{"--models"}</code></nobr>  | عرض تفصيل استخدام النماذج (مخفي افتراضيا). مرّر رقمًا لعرض أعلى N         |
| <nobr><code>{"--project"}</code></nobr> | التصفية حسب المشروع (الافتراضي: كل المشاريع، سلسلة فارغة: المشروع الحالي) |

---

### export

تصدير بيانات الجلسة بصيغة JSON.

```bash
openhack export [sessionID]
```

إذا لم تُقدّم معرّف جلسة، فسيُطلب منك الاختيار من الجلسات المتاحة.

---

### import

استيراد بيانات الجلسة من ملف JSON أو رابط مشاركة OpenHack.

```bash
openhack import <file>
```

يمكنك الاستيراد من ملف محلي أو من رابط مشاركة OpenHack.

```bash
openhack import session.json
openhack import https://opncd.ai/s/abc123
```

---

### web

بدء خادم OpenHack بدون واجهة مع واجهة ويب.

```bash
openhack web
```

يشغّل هذا خادم HTTP ويفتح متصفح ويب للوصول إلى OpenHack عبر واجهة ويب. اضبط `OPENHACK_SERVER_PASSWORD` لتفعيل مصادقة HTTP الأساسية (اسم المستخدم الافتراضي `openhack`).

#### الرايات

| الراية                                   | الوصف                                     |
| ---------------------------------------- | ----------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | المنفذ الذي يتم الاستماع عليه             |
| <nobr><code>{"--hostname"}</code></nobr> | اسم المضيف الذي يتم الاستماع عليه         |
| <nobr><code>{"--mdns"}</code></nobr>     | تفعيل اكتشاف mDNS                         |
| <nobr><code>{"--cors"}</code></nobr>     | أصول/منشأات إضافية للمتصفح للسماح بـ CORS |

---

### acp

بدء خادم ACP (Agent Client Protocol).

```bash
openhack acp
```

يشغّل هذا الأمر خادم ACP يتواصل عبر stdin/stdout باستخدام nd-JSON.

#### الرايات

| الراية                                   | الوصف                             |
| ---------------------------------------- | --------------------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | دليل العمل                        |
| <nobr><code>{"--port"}</code></nobr>     | المنفذ الذي يتم الاستماع عليه     |
| <nobr><code>{"--hostname"}</code></nobr> | اسم المضيف الذي يتم الاستماع عليه |

---

### uninstall

إلغاء تثبيت OpenHack وإزالة جميع الملفات ذات الصلة.

```bash
openhack uninstall
```

#### الرايات

| الراية                                      | المختصر | الوصف                               |
| ------------------------------------------- | ------- | ----------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`    | الإبقاء على ملفات التهيئة           |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`    | الإبقاء على بيانات الجلسات واللقطات |
| <nobr><code>{"--dry-run"}</code></nobr>     |         | عرض ما سيتم حذفه دون تنفيذ الحذف    |
| <nobr><code>{"--force"}</code></nobr>       | `-f`    | تخطي مطالبات التأكيد                |

---

### upgrade

تحديث openhack إلى أحدث إصدار أو إلى إصدار محدد.

```bash
openhack upgrade [target]
```

للترقية إلى أحدث إصدار.

```bash
openhack upgrade
```

للترقية إلى إصدار محدد.

```bash
openhack upgrade v0.1.48
```

#### الرايات

| الراية                                 | المختصر | الوصف                                                       |
| -------------------------------------- | ------- | ----------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`    | طريقة التثبيت المستخدمة: curl أو npm أو pnpm أو bun أو brew |

---

## الخيارات العامة

يدعم سطر أوامر openhack الخيارات العامة التالية.

| الراية                                     | المختصر | الوصف                                  |
| ------------------------------------------ | ------- | -------------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h`    | عرض المساعدة                           |
| <nobr><code>{"--version"}</code></nobr>    | `-v`    | طباعة رقم الإصدار                      |
| <nobr><code>{"--print-logs"}</code></nobr> |         | طباعة السجلات إلى stderr               |
| <nobr><code>{"--log-level"}</code></nobr>  |         | مستوى السجل (DEBUG, INFO, WARN, ERROR) |

---

## متغيرات البيئة

يمكن تهيئة OpenHack باستخدام متغيرات البيئة.

| المتغير                               | النوع   | الوصف                                                     |
| ------------------------------------- | ------- | --------------------------------------------------------- |
| `OPENHACK_AUTO_SHARE`                 | boolean | مشاركة الجلسات تلقائيا                                    |
| `OPENHACK_GIT_BASH_PATH`              | string  | مسار ملف Git Bash التنفيذي على Windows                    |
| `OPENHACK_CONFIG`                     | string  | مسار ملف التهيئة                                          |
| `OPENHACK_CONFIG_DIR`                 | string  | مسار دليل التهيئة                                         |
| `OPENHACK_CONFIG_CONTENT`             | string  | محتوى تهيئة JSON مُضمّن                                   |
| `OPENHACK_DISABLE_AUTOUPDATE`         | boolean | تعطيل التحقق التلقائي من التحديثات                        |
| `OPENHACK_DISABLE_PRUNE`              | boolean | تعطيل تنقية البيانات القديمة                              |
| `OPENHACK_DISABLE_TERMINAL_TITLE`     | boolean | تعطيل تحديث عنوان terminal تلقائيا                        |
| `OPENHACK_PERMISSION`                 | string  | تهيئة أذونات JSON مُضمّنة                                 |
| `OPENHACK_DISABLE_DEFAULT_PLUGINS`    | boolean | تعطيل الإضافات الافتراضية                                 |
| `OPENHACK_DISABLE_LSP_DOWNLOAD`       | boolean | تعطيل تنزيل خوادم LSP تلقائيا                             |
| `OPENHACK_ENABLE_EXPERIMENTAL_MODELS` | boolean | تفعيل النماذج التجريبية                                   |
| `OPENHACK_DISABLE_AUTOCOMPACT`        | boolean | تعطيل ضغط السياق تلقائيا                                  |
| `OPENHACK_DISABLE_CLAUDE_CODE`        | boolean | تعطيل القراءة من `.claude` (الموجه + المهارات)            |
| `OPENHACK_DISABLE_CLAUDE_CODE_PROMPT` | boolean | تعطيل قراءة `~/.claude/CLAUDE.md`                         |
| `OPENHACK_DISABLE_CLAUDE_CODE_SKILLS` | boolean | تعطيل تحميل `.claude/skills`                              |
| `OPENHACK_DISABLE_MODELS_FETCH`       | boolean | تعطيل جلب النماذج من مصادر بعيدة                          |
| `OPENHACK_FAKE_VCS`                   | string  | مزود VCS وهمي لأغراض الاختبار                             |
| `OPENHACK_CLIENT`                     | string  | معرّف العميل (الافتراضي `cli`)                            |
| `OPENHACK_ENABLE_EXA`                 | boolean | تفعيل أدوات بحث الويب من Exa                              |
| `OPENHACK_SERVER_PASSWORD`            | string  | تفعيل المصادقة الأساسية لخادمي `serve`/`web`              |
| `OPENHACK_SERVER_USERNAME`            | string  | تجاوز اسم مستخدم المصادقة الأساسية (الافتراضي `openhack`) |
| `OPENHACK_MODELS_URL`                 | string  | رابط مخصص لجلب تهيئة النماذج                              |

---

### تجريبي

تقوم متغيرات البيئة التالية بتفعيل ميزات تجريبية قد تتغير أو تتم إزالتها.

| المتغير                                         | النوع   | الوصف                                        |
| ----------------------------------------------- | ------- | -------------------------------------------- |
| `OPENHACK_EXPERIMENTAL`                         | boolean | تفعيل الميزات التجريبية الخاضعة للعلم الشامل |
| `OPENHACK_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | تفعيل اكتشاف الأيقونات                       |
| `OPENHACK_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | تعطيل النسخ عند التحديد في TUI               |
| `OPENHACK_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | المهلة الافتراضية لأوامر bash بالميلي ثانية  |
| `OPENHACK_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | الحد الأقصى لرموز المخرجات لاستجابات LLM     |
| `OPENHACK_EXPERIMENTAL_FILEWATCHER`             | boolean | تفعيل مراقب الملفات لكامل الدليل             |
| `OPENHACK_EXPERIMENTAL_OXFMT`                   | boolean | تفعيل مُنسّق oxfmt                           |
| `OPENHACK_EXPERIMENTAL_LSP_TOOL`                | boolean | تفعيل أداة LSP تجريبية                       |
| `OPENHACK_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | تعطيل مراقب الملفات                          |
| `OPENHACK_EXPERIMENTAL_EXA`                     | boolean | تفعيل ميزات Exa التجريبية                    |
| `OPENHACK_EXPERIMENTAL_LSP_TY`                  | boolean | تمكين TY LSP لملفات python                   |
| `OPENHACK_EXPERIMENTAL_PLAN_MODE`               | boolean | تفعيل وضع الخطة                              |
| `OPENHACK_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | تفعيل مهام subagent في الخلفية               |
| `OPENHACK_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | تفعيل نظام الأحداث التجريبي                  |
| `OPENHACK_EXPERIMENTAL_NATIVE_LLM`              | boolean | تفعيل مسار طلبات LLM الأصلي                  |
| `OPENHACK_EXPERIMENTAL_PARALLEL`                | boolean | تفعيل تنفيذ بحث الويب بالتوازي               |
| `OPENHACK_EXPERIMENTAL_SCOUT`                   | boolean | تفعيل Scout subagent                         |
| `OPENHACK_EXPERIMENTAL_WORKSPACES`              | boolean | تفعيل دعم مساحات العمل                       |
