---
title: CLI
description: OpenHack CLI-alternativer og kommandoer.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

OpenHack CLI starter som standard [TUI](/docs/tui) når den kjøres uten noen argumenter.

```bash
openhack
```

Men den godtar også kommandoer som dokumentert på denne siden. Dette lar deg samhandle med OpenHack programmatisk.

```bash
openhack run "Explain how closures work in JavaScript"
```

---

### tui

Start OpenHack-terminalbrukergrensesnittet.

```bash
openhack [project]
```

#### Flagg

| Flagg                                    | Kort | Beskrivelse                                                              |
| ---------------------------------------- | ---- | ------------------------------------------------------------------------ |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Fortsett siste økt                                                       |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Økt ID for å fortsette                                                   |
| <nobr><code>{"--fork"}</code></nobr>     |      | Forgren økten ved fortsettelse (bruk med `--continue` eller `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |      | Ledetekst som skal brukes                                                |
| <nobr><code>{"--model"}</code></nobr>    | `-m` | Modell å bruke i form av leverandør/modell                               |
| <nobr><code>{"--agent"}</code></nobr>    |      | Agent som skal brukes                                                    |
| <nobr><code>{"--port"}</code></nobr>     |      | Port å lytte på                                                          |
| <nobr><code>{"--hostname"}</code></nobr> |      | Vertsnavn å lytte på                                                     |

---

## Kommandoer

OpenHack CLI har også følgende kommandoer.

---

### agent

Administrer agenter for OpenHack.

```bash
openhack agent [command]
```

---

### attach

Koble en terminal til en allerede kjørende OpenHack-backend-server startet via `serve` eller `web` kommandoer.

```bash
openhack attach [url]
```

Dette gjør det mulig å bruke TUI med en ekstern OpenHack-backend. For eksempel:

```bash
# Start the backend server for web/mobile access
openhack web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
openhack attach http://10.20.30.40:4096
```

#### Flagg

| Flagg                                    | Kort | Beskrivelse                                                                                        |
| ---------------------------------------- | ---- | -------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |      | Arbeidskatalog for å starte TUI i                                                                  |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Fortsett siste økt                                                                                 |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Økt ID for å fortsette                                                                             |
| <nobr><code>{"--fork"}</code></nobr>     |      | Forgren økten ved fortsettelse (bruk med `--continue` eller `--session`)                           |
| <nobr><code>{"--password"}</code></nobr> | `-p` | Passord for grunnleggende autentisering (standard: `OPENHACK_SERVER_PASSWORD`)                     |
| <nobr><code>{"--username"}</code></nobr> | `-u` | Brukernavn for grunnleggende autentisering (standard: `OPENHACK_SERVER_USERNAME` eller `openhack`) |

---

#### create

Opprett en ny agent med tilpasset konfigurasjon.

```bash
openhack agent create
```

Denne kommandoen vil veilede deg gjennom å lage en ny agent med en tilpasset systemmelding og verktøykonfigurasjon.

---

#### list

List opp alle tilgjengelige agenter.

```bash
openhack agent list
```

---

### auth

Kommando for administrasjon av påloggingsinformasjon og innlogging for leverandører.

```bash
openhack auth [command]
```

---

#### login

OpenHack drives av leverandørlisten på [Models.dev](https://models.dev), så du kan bruke `openhack auth login` til å konfigurere API-nøkler for enhver leverandør du vil bruke. Dette er lagret i `~/.local/share/openhack/auth.json`.

```bash
openhack auth login
```

Når OpenHack starter opp, laster den leverandørene fra filen med påloggingsinformasjon. Og hvis det er noen nøkler definert i miljøene dine eller en `.env`-fil i prosjektet ditt.

---

#### list

Viser alle de autentiserte leverandørene som er lagret i filen med påloggingsinformasjon.

```bash
openhack auth list
```

Eller bruk kortversjonen.

```bash
openhack auth ls
```

---

#### logout

Logger deg ut av en leverandør ved å fjerne den fra filen med påloggingsinformasjon.

```bash
openhack auth logout
```

---

### github

Administrer GitHub-agenten for depotautomatisering.

```bash
openhack github [command]
```

---

#### install

Installer GitHub-agenten i depotet ditt.

```bash
openhack github install
```

Dette setter opp den nødvendige GitHub Actions-arbeidsflyten og veileder deg gjennom konfigurasjonsprosessen. [Finn ut mer](/docs/github).

---

#### run

Kjør GitHub-agenten. Dette brukes vanligvis i GitHub Actions.

```bash
openhack github run
```

##### Flagg

| Flagg                                 | Beskrivelse                                  |
| ------------------------------------- | -------------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub mock-hendelse agenten skal kjøres for |
| <nobr><code>{"--token"}</code></nobr> | GitHub personlig tilgangsnøkkel              |

---

### mcp

Administrer Model Context Protocol-servere.

```bash
openhack mcp [command]
```

---

#### add

Legg til en MCP-server til konfigurasjonen.

```bash
openhack mcp add
```

Denne kommandoen vil veilede deg gjennom å legge til enten en lokal eller ekstern MCP-server.

---

#### list

Liste alle konfigurerte MCP-servere og deres tilkoblingsstatus.

```bash
openhack mcp list
```

Eller bruk den korte versjonen.

```bash
openhack mcp ls
```

---

#### auth

Autentiser med en OAuth-aktivert MCP-server.

```bash
openhack mcp auth [name]
```

Hvis du ikke oppgir et servernavn, blir du bedt om å velge blant tilgjengelige OAuth-kompatible servere.

Du kan også liste opp OAuth-kompatible servere og deres autentiseringsstatus.

```bash
openhack mcp auth list
```

Eller bruk den korte versjonen.

```bash
openhack mcp auth ls
```

---

#### logout

Fjern OAuth-påloggingsinformasjon for en MCP-server.

```bash
openhack mcp logout [name]
```

---

#### debug

Feilsøk OAuth-tilkoblingsproblemer for en MCP-server.

```bash
openhack mcp debug <name>
```

---

### models

List opp alle tilgjengelige modeller fra konfigurerte leverandører.

```bash
openhack models [provider]
```

Denne kommandoen viser alle modeller som er tilgjengelige på tvers av dine konfigurerte leverandører i formatet `provider/model`.

Dette er nyttig for å finne ut det eksakte modellnavnet som skal brukes i [din konfigurasjon](/docs/config/).

Du kan eventuelt sende en leverandør ID for å filtrere modeller etter den leverandøren.

```bash
openhack models anthropic
```

#### Flagg

| Flagg                                   | Beskrivelse                                                         |
| --------------------------------------- | ------------------------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Oppdater modellbufferen fra models.dev                              |
| <nobr><code>{"--verbose"}</code></nobr> | Bruk mer detaljert modellutdata (inkluderer metadata som kostnader) |

Bruk `--refresh`-flagget for å oppdatere den bufrede modelllisten. Dette er nyttig når nye modeller er lagt til en leverandør og du vil se dem i OpenHack.

```bash
openhack models --refresh
```

---

### run

Kjør OpenHack i ikke-interaktiv modus ved å sende en melding direkte.

```bash
openhack run [message..]
```

Dette er nyttig for skripting, automatisering eller når du vil ha et raskt svar uten å starte hele TUI. For eksempel.

```bash "openhack run"
openhack run Explain the use of context in Go
```

Du kan også koble til en kjørende `openhack serve`-forekomst for å unngå MCP-serveres kaldstarttid ved hver kjøring:

```bash
# Start a headless server in one terminal
openhack serve

# In another terminal, run commands that attach to it
openhack run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flagg

| Flagg                                    | Kort | Beskrivelse                                                                                        |
| ---------------------------------------- | ---- | -------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |      | Kommandoen for å kjøre, bruk melding for args                                                      |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Fortsett siste økt                                                                                 |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Økt ID for å fortsette                                                                             |
| <nobr><code>{"--fork"}</code></nobr>     |      | Forgren økten ved fortsettelse (bruk med `--continue` eller `--session`)                           |
| <nobr><code>{"--share"}</code></nobr>    |      | Del økten                                                                                          |
| <nobr><code>{"--model"}</code></nobr>    | `-m` | Modell å bruke i form av leverandør/modell                                                         |
| <nobr><code>{"--agent"}</code></nobr>    |      | Agent å bruke                                                                                      |
| <nobr><code>{"--file"}</code></nobr>     | `-f` | Fil(er) som skal legges ved meldingen                                                              |
| <nobr><code>{"--format"}</code></nobr>   |      | Format: standard (formatert) eller json (rå JSON hendelser)                                        |
| <nobr><code>{"--title"}</code></nobr>    |      | Tittel for økten (bruker avkortet ledetekst hvis ingen verdi er oppgitt)                           |
| <nobr><code>{"--attach"}</code></nobr>   |      | Koble til en kjørende OpenHack-server (f.eks. http://localhost:4096)                               |
| <nobr><code>{"--password"}</code></nobr> | `-p` | Passord for grunnleggende autentisering (standard: `OPENHACK_SERVER_PASSWORD`)                     |
| <nobr><code>{"--username"}</code></nobr> | `-u` | Brukernavn for grunnleggende autentisering (standard: `OPENHACK_SERVER_USERNAME` eller `openhack`) |
| <nobr><code>{"--dir"}</code></nobr>      |      | Katalog å kjøre i, eller sti på fjernserveren ved tilkobling                                       |
| <nobr><code>{"--variant"}</code></nobr>  |      | Modellvariant (leverandørspesifikk resonneringsinnsats)                                            |
| <nobr><code>{"--thinking"}</code></nobr> |      | Vis tenkeblokker                                                                                   |
| <nobr><code>{"--port"}</code></nobr>     |      | Port for den lokale serveren (standard til tilfeldig port)                                         |

---

### serve

Start en headless OpenHack-server for API-tilgang. Sjekk ut [server-dokumentene](/docs/server) for hele HTTP-grensesnittet.

```bash
openhack serve
```

Dette starter en HTTP-server som gir API tilgang til OpenHack-funksjonalitet uten TUI-grensesnittet. Sett `OPENHACK_SERVER_PASSWORD` for å aktivere HTTP grunnleggende autentisering (brukernavn er standard til `openhack`).

#### Flagg

| Flagg                                    | Beskrivelse                                        |
| ---------------------------------------- | -------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Port å lytte på                                    |
| <nobr><code>{"--hostname"}</code></nobr> | Vertsnavn å lytte på                               |
| <nobr><code>{"--mdns"}</code></nobr>     | Aktiver mDNS-oppdagelse                            |
| <nobr><code>{"--cors"}</code></nobr>     | Ytterligere nettleseropprinnelse som tillater CORS |

---

### session

Administrer OpenHack-økter.

```bash
openhack session [command]
```

---

#### list

List opp alle OpenHack-økter.

```bash
openhack session list
```

##### Flagg

| Flagg                                     | Kort | Beskrivelse                              |
| ----------------------------------------- | ---- | ---------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n` | Begrens til N siste økter                |
| <nobr><code>{"--format"}</code></nobr>    |      | Utdataformat: tabell eller json (tabell) |

---

### stats

Vis tokenbruk og kostnadsstatistikk for OpenHack-øktene dine.

```bash
openhack stats
```

#### Flagg

| Flagg                                   | Beskrivelse                                                                      |
| --------------------------------------- | -------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Vis statistikk for de siste N dagene (hele tiden)                                |
| <nobr><code>{"--tools"}</code></nobr>   | Antall verktøy som skal vises (alle)                                             |
| <nobr><code>{"--models"}</code></nobr>  | Vis oversikt over modellbruk (skjult som standard). Gi et tall for å vise topp N |
| <nobr><code>{"--project"}</code></nobr> | Filtrer etter prosjekt (alle prosjekter, tom streng: gjeldende prosjekt)         |

---

### export

Eksporter øktdata som JSON.

```bash
openhack export [sessionID]
```

Hvis du ikke gir en økt ID, blir du bedt om å velge fra tilgjengelige økter.

---

### import

Importer øktdata fra en JSON-fil eller en OpenHack share-URL.

```bash
openhack import <file>
```

Du kan importere fra en lokal fil eller en OpenHack share-URL.

```bash
openhack import session.json
openhack import https://opncd.ai/s/abc123
```

---

### web

Start en headless OpenHack-server med et webgrensesnitt.

```bash
openhack web
```

Dette starter en HTTP-server og åpner en nettleser for å få tilgang til OpenHack via et nettgrensesnitt. Sett `OPENHACK_SERVER_PASSWORD` for å aktivere HTTP grunnleggende autentisering (brukernavn er standard til `openhack`).

#### Flagg

| Flagg                                    | Beskrivelse                                        |
| ---------------------------------------- | -------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Port å lytte på                                    |
| <nobr><code>{"--hostname"}</code></nobr> | Vertsnavn å lytte på                               |
| <nobr><code>{"--mdns"}</code></nobr>     | Aktiver mDNS-oppdagelse                            |
| <nobr><code>{"--cors"}</code></nobr>     | Ytterligere nettleseropprinnelse som tillater CORS |

---

### acp

Start en ACP (Agent Client Protocol) server.

```bash
openhack acp
```

Denne kommandoen starter en ACP-server som kommuniserer via stdin/stdout ved å bruke nd-JSON.

#### Flagg

| Flagg                                    | Beskrivelse          |
| ---------------------------------------- | -------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Arbeidskatalog       |
| <nobr><code>{"--port"}</code></nobr>     | Port å lytte på      |
| <nobr><code>{"--hostname"}</code></nobr> | Vertsnavn å lytte på |

---

### uninstall

Avinstaller OpenHack og fjern alle relaterte filer.

```bash
openhack uninstall
```

#### Flagg

| Flagg                                       | Kort | Beskrivelse                                   |
| ------------------------------------------- | ---- | --------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c` | Behold konfigurasjonsfiler                    |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d` | Behold øktdata og øyeblikksbilder             |
| <nobr><code>{"--dry-run"}</code></nobr>     |      | Vis hva som ville blitt fjernet uten å fjerne |
| <nobr><code>{"--force"}</code></nobr>       | `-f` | Hopp over bekreftelsesforespørsler            |

---

### upgrade

Oppdaterer OpenHack til siste versjon eller en spesifikk versjon.

```bash
openhack upgrade [target]
```

For å oppgradere til siste versjon.

```bash
openhack upgrade
```

For å oppgradere til en bestemt versjon.

```bash
openhack upgrade v0.1.48
```

#### Flagg

| Flagg                                  | Kort | Beskrivelse                                                    |
| -------------------------------------- | ---- | -------------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m` | Installasjonsmetoden som ble brukt: curl, npm, pnpm, bun, brew |

---

## Globale flagg

OpenHack CLI bruker følgende globale flagg.

| Flagg                                      | Kort | Beskrivelse                         |
| ------------------------------------------ | ---- | ----------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h` | Vis hjelp                           |
| <nobr><code>{"--version"}</code></nobr>    | `-v` | Skriv ut versjonsnummer             |
| <nobr><code>{"--print-logs"}</code></nobr> |      | Skriv ut logger til stderr          |
| <nobr><code>{"--log-level"}</code></nobr>  |      | Loggnivå (DEBUG, INFO, WARN, ERROR) |

---

## Miljøvariabler

OpenHack kan konfigureres ved hjelp av miljøvariabler.

| Variabel                              | Type   | Beskrivelse                                                           |
| ------------------------------------- | ------ | --------------------------------------------------------------------- |
| `OPENHACK_AUTO_SHARE`                 | boolsk | Del økter automatisk                                                  |
| `OPENHACK_GIT_BASH_PATH`              | streng | Bane til Git Bash-kjørbar på Windows                                  |
| `OPENHACK_CONFIG`                     | streng | Bane til konfigurasjonsfil                                            |
| `OPENHACK_TUI_CONFIG`                 | streng | Bane til TUI-konfigurasjonsfil                                        |
| `OPENHACK_CONFIG_DIR`                 | streng | Bane til konfigurasjonskatalog                                        |
| `OPENHACK_CONFIG_CONTENT`             | streng | Innebygd json-konfigurasjonsinnhold                                   |
| `OPENHACK_DISABLE_AUTOUPDATE`         | boolsk | Deaktiver automatiske oppdateringskontroller                          |
| `OPENHACK_DISABLE_PRUNE`              | boolsk | Deaktiver beskjæring av gamle data                                    |
| `OPENHACK_DISABLE_TERMINAL_TITLE`     | boolsk | Deaktiver automatiske terminaltitteloppdateringer                     |
| `OPENHACK_PERMISSION`                 | streng | Innebygd json-tillatelseskonfigurasjon                                |
| `OPENHACK_DISABLE_DEFAULT_PLUGINS`    | boolsk | Deaktiver standard plugins                                            |
| `OPENHACK_DISABLE_LSP_DOWNLOAD`       | boolsk | Deaktiver automatiske LSP servernedlastinger                          |
| `OPENHACK_ENABLE_EXPERIMENTAL_MODELS` | boolsk | Aktiver eksperimentelle modeller                                      |
| `OPENHACK_DISABLE_AUTOCOMPACT`        | boolsk | Deaktiver automatisk kontekstkomprimering                             |
| `OPENHACK_DISABLE_CLAUDE_CODE`        | boolsk | Deaktiver lesing fra `.claude` (spørsmål + ferdigheter)               |
| `OPENHACK_DISABLE_CLAUDE_CODE_PROMPT` | boolsk | Deaktiver lesing `~/.claude/CLAUDE.md`                                |
| `OPENHACK_DISABLE_CLAUDE_CODE_SKILLS` | boolsk | Deaktiver innlasting av `.claude/skills`                              |
| `OPENHACK_DISABLE_MODELS_FETCH`       | boolsk | Deaktiver henting av modeller fra eksterne kilder                     |
| `OPENHACK_FAKE_VCS`                   | streng | Falsk VCS-leverandør for testformål                                   |
| `OPENHACK_CLIENT`                     | streng | Klientidentifikator (standard til `cli`)                              |
| `OPENHACK_ENABLE_EXA`                 | boolsk | Aktiver Exa-nettsøkeverktøy                                           |
| `OPENHACK_SERVER_PASSWORD`            | streng | Aktiver grunnleggende autentisering for `serve`/`web`                 |
| `OPENHACK_SERVER_USERNAME`            | streng | Overstyr grunnleggende autentiseringsbrukernavn (standard `openhack`) |
| `OPENHACK_MODELS_URL`                 | streng | Egendefinert URL for henting av modellkonfigurasjon                   |

---

### Eksperimentelt

Disse miljøvariablene muliggjør eksperimentelle funksjoner som kan endres eller fjernes.

| Variabel                                        | Type   | Beskrivelse                                           |
| ----------------------------------------------- | ------ | ----------------------------------------------------- |
| `OPENHACK_EXPERIMENTAL`                         | boolsk | Aktiver eksperimentelle funksjoner under samleflagget |
| `OPENHACK_EXPERIMENTAL_ICON_DISCOVERY`          | boolsk | Aktiver ikonoppdagelse                                |
| `OPENHACK_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolsk | Deaktiver kopi ved valg i TUI                         |
| `OPENHACK_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | tall   | Standard tidsavbrudd for bash-kommandoer i ms         |
| `OPENHACK_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | tall   | Maks Output Tokens for LLM-svar                       |
| `OPENHACK_EXPERIMENTAL_FILEWATCHER`             | boolsk | Aktiver filovervåker for hele dir                     |
| `OPENHACK_EXPERIMENTAL_OXFMT`                   | boolsk | Aktiver oxfmt formatter                               |
| `OPENHACK_EXPERIMENTAL_LSP_TOOL`                | boolsk | Aktiver eksperimentelt LSP-verktøy                    |
| `OPENHACK_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolsk | Deaktiver filovervåking                               |
| `OPENHACK_EXPERIMENTAL_EXA`                     | boolsk | Aktiver eksperimentelle Exa-funksjoner                |
| `OPENHACK_EXPERIMENTAL_LSP_TY`                  | boolsk | Aktiver TY LSP for python-filer                       |
| `OPENHACK_EXPERIMENTAL_PLAN_MODE`               | boolsk | Aktiver planmodus                                     |
| `OPENHACK_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolsk | Aktiver bakgrunnsoppgaver for subagenter              |
| `OPENHACK_EXPERIMENTAL_EVENT_SYSTEM`            | boolsk | Aktiver eksperimentelt hendelsessystem                |
| `OPENHACK_EXPERIMENTAL_NATIVE_LLM`              | boolsk | Aktiver innebygd LLM-forespørselsvei                  |
| `OPENHACK_EXPERIMENTAL_PARALLEL`                | boolsk | Aktiver parallell kjøring av websøk                   |
| `OPENHACK_EXPERIMENTAL_SCOUT`                   | boolsk | Aktiver Scout-subagent                                |
| `OPENHACK_EXPERIMENTAL_WORKSPACES`              | boolsk | Aktiver arbeidsområde-støtte                          |
